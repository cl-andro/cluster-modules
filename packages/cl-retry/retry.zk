// cl-retry: Operations Retry Engine

fn get_backoff(attempt: int, initial_delay_ms: int) -> int:
    delay := initial_delay_ms
    i := 1
    while i < attempt:
        delay = delay * 2
        i = i + 1
    return delay
